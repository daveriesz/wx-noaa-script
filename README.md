# wx-noaa-script

This is a basic bash script to fetch weather data for locations in the USA from api.weather.gov.
Location geocoding comes from OpenStreetmap and is cached locally in `$HOME/.wx-loc-cache`.

Peruse the `wx` script to discover options.  I'll document it later.

MIT license.
